//itn a = feltétel (bool) ? = 5; 2;
//$"valami"



/*
using System.Data;

bool runloop = true;
int summa = 0;
int numbercount = 0;

while (runloop)
{
    string line = Console.ReadLine();

    bool success = int.TryParse(line,out int number);
    
    if(success)
    {
        summa += number;
        numbercount++;
    }
    else
    {
        break;
    }

}
float átlag =(float)summa/numbercount;

Console.WriteLine($"{átlag}");

//2
float pi = (float)Math.PI;
const float pi2 = 3.14f;//const nem lehet megváltoztatni (maradando)
double yal = Math.Pow(5, 6);
double sqrt= Math.Sqrt(56);
double sqrt = Math.Pow(5, 1/6f);
*/
