using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.Animations;

public class Autorotate : MonoBehaviour
{
    /*
    [SerializeField] float angrot= 360;
    [SerializeField] Vector3 axes = new Vector3.up;
    [SerializeField] Space rotspace;
    [SerializeField] float range = 10;

     void Update()
    {
        float distence = Vector3.Distance(selfpos,targetpos)
        Vector3 auler = new Vector3(0, angrot, 0);
        auler *= Time.deltaTime;
       
        transform.Rotate(axes, angrot * Time.deltaTime ,rotspace);
           void OnDrawGizmos()
        {
            Vector3 center = transform.position;

            Vector3 dir;
            if (rotspace== Space.World)
            {
                dir = Axis.normalized;
                else
                {
                    dir = transform.transformDirection(axis).normalize;
                }
            }
            Gizmos.color = Color.yellow;
            Gizmos.DrawLine;

        }
    }
    */
}
